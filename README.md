# Eats

This is an application to save your favorite restaurant and also view your saved restraurants.

## Installation / Run

#### Tech stack

Javascript, React, NodeJS, Express, PostgreSQL, Docker

#### Install packages

`docker-compose build`

#### To launch the application. (virtual environment must be instantiated)

`docker-compose up`
Go to `http://localhost:8000` to test the application.
